# Concepts that i learn from this project :

__init__.py : special file used to define packeges and initialize their namespaces. It can contain an initialization code that run when the package is imported.



SQLAlchemy : is Object Relational Mapper (ORM) that allows developers to interact with relational databases using python objects insted of writing raw SQl query.
Alembic : is database migration tool used primarily with SQLAlchemy.
Pydantic : is data validation and parsing libirary.

Repository Pattern : isolates all SQLAlchemy code into one specific file


LLM :
AI agent :
Embadding :
Chunks :
Vector DB :